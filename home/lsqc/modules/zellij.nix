{ ... }: {

  programs.zellij = {
    enable = true;

    settings = {
      # theme = "custom";
      # themes.custom = {
      #   fg = "#ffffff";
      #   bg = "#000000";
      # };
    };
  };
}
